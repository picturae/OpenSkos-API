parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        phplint: ~
        phpcsfixer2:
            allow_risky: true
            config: .php_cs
        psalm:
            ignore_patterns:
                - '/spec/' #Ignore PHPSpec
        phpspec: ~
        shell:
          scripts:
            - scripts/generate-documentation.sh
